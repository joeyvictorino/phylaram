#pragma once

#include <windows.h>

// Launches the native Windows 11 Fluent dark-mode GUI.
// Provides 1-click physical memory capture for on-site sysadmins & incident responders.
int LaunchGui(HINSTANCE hInstance);
